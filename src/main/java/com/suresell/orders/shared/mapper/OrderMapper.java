package com.suresell.orders.shared.mapper;

// This interface would typically define methods for mapping between DTOs and domain models.
// Specific methods and their implementations will depend on the application's needs.
public interface OrderMapper {
    // Example:
    // import com.suresell.orders.application.dto.OrderRequestRecord;
    // import com.suresell.orders.application.dto.OrderResponseRecord;
    // import com.suresell.orders.domain.model.Order;
    // Order toDomain(OrderRequestRecord orderRequestRecord);
    // OrderResponseRecord toDto(Order order);
}
